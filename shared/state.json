{
  "$comment": "Single source of truth for packed state, thus never edit the generated files, edit this one and run `npm run gen:state` from vite/. the ci fails if the generated output drifts from this file.",
  "enums": {
    "RunState": {
      "description": "Where the game as a whole is; replaces sniffing scene paths on the React side.",
      "values": {
        "BOOTING": 0,
        "MENU": 1,
        "PLAYING": 2,
        "PAUSED": 3,
        "ENDED": 4
      }
    }
  },
  "flags": {
    "PlayerFlags": {
      "description": "Per frame player state! Packed because systems test these thousands of times a second; a bit test is a single AND against a register but overkill for now, damn safari.",
      "bits": {
        "ALIVE": 0,
        "MOVING": 1,
        "ATTACKING": 2,
        "INVULNERABLE": 3
      }
    }
  }
}
